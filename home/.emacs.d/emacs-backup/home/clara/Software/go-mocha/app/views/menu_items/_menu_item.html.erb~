<form id='customize' data-remote='true'      action='/add_to_cart/<%= @menu_item.id %>'>
        <h3>Customize</h3>
              <div class="customMenuItem">
                <span class="customName"><%= @menu_item.name                %></span><span class="menuPrice"><%= number_to_currency                @menu_item.price %></span>
                <span class="customDesc">A perfect blend of mocha and espresso, with whip cream.</span>

<!--                <div class="customs">Select Size
                  <select>
                    <option>Small ($2.99)</option>
                    <option>Medium ($3.99)</option>
                    <option>Large ($4.99)</option>
                    <option>Extra Large ($5.99)</option>
                  </select>
                </div>
                <div class="customs">Milk
                  <select>
                    <option>Non-Fat</option>
                    <option>2% Fat</option>
                    <option>Whole Milk</option>
                    <option>Soy Milk (+$1.00)</option>
                  </select>
                </div>
-->

                <div class="customs">
                   <table class="addon">
<% for ao in @menu_item.add_ons %>
         <tr>
<td> <%= check_box_tag  "add_on_ids[]", ao.id %> <span>
          <%= ao.description %> (<%= number_to_currency ao.price          %>)</span></td><td></td></tr>
        <% end %>
         
                </div>
                <div class="customs">Special Instructions
          <%= text_field_tag "notes" %>
                </div>
                <div class="customs">
                  <a href="#" class="orderCancel">Remove Item <i class="fa fa-minus-circle fa-1x white iconMargin"></i></a>
                  <a href="#" onclick="$('customize').submit()" class="orderButton">Add to Order<i class="fa fa-plus fa-1x white iconMargin"></i></a>
                </div>
                </div>

</form>
