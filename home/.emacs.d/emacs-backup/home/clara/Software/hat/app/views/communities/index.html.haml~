-content_for :header do
  .header.job_full.communities
    %h1.job_title
      Community Resources


    .btn-group.pull-right{:style => 'margin-top: 6px;'}
      .btn.dropdown-toggle
        See Other Towns
        %span.caret
      %ul.dropdown-menu
        - Community.all.each do |community|
          %li= link_to community.name, community_path(community.slug)
    
    - if current_user.try :is_admin
      =link_to 'edit', edit_admin_community_path(@community.id), :class => 'bg-blue fg-white', :id => 'admin_edit', :style => 'float: right;'
      %script $('#admin_edit').button();

.static_text= @txt.html_safe
